services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: asset_manager
    ports:
      - "5432:5432"
    volumes:
      - asset_manager_pgdata:/var/lib/postgresql/data

volumes:
  asset_manager_pgdata:

